# Architecture — Ultimate Playbook

## 1. Stack technique

| Domaine          | Choix                        | Justification                                                                                                                                           |
| ---------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Build tool       | **Vite**                     | Démarrage rapide, config simple, bon support GitHub Pages via `base`.                                                                                   |
| UI               | **React 18 + TypeScript**    | Écosystème large, typage utile pour un modèle de données structuré (frames, entités).                                                                   |
| Rendu terrain    | **Konva.js** (`react-konva`) | Gère nativement drag & drop, pointer events (souris + tactile), animation/tween — correspond exactement au besoin (édition + lecture pas-à-pas/fluide). |
| State management | **Zustand**                  | Léger, suffisant pour l'état d'édition + bibliothèque locale, pas besoin de Redux.                                                                      |
| i18n             | **react-i18next**            | Structure en place dès le MVP (FR complet, EN en fallback FR), voir PRD §4.6.                                                                           |
| Tests unitaires  | **Vitest** + Testing Library | Cohérent avec Vite, rapide.                                                                                                                             |
| Lint / format    | **ESLint + Prettier**        | Standard.                                                                                                                                               |
| Hébergement MVP  | **GitHub Pages**             | Statique, gratuit, suffisant tant qu'il n'y a pas de backend.                                                                                           |

Aucun backend au MVP (voir `docs/PRD.md` §4.5). L'architecture doit rester **agnostique de l'hébergement** : pas de dépendance à une spécificité GitHub Pages dans le code applicatif (le `base` path et le workflow de déploiement sont isolés dans la config Vite / CI, pas dans le code métier), pour permettre une migration facile vers Netlify/Vercel/Supabase Hosting plus tard.

## 2. Structure de projet (cible)

```
ultimate-playbook/
├── docs/                      # ce dossier — spécifications
├── public/
├── src/
│   ├── domain/                # modèles TS + logique pure (pas de dépendance React)
│   │   ├── models.ts          # Action, Frame, Entity, FieldConfig, Disc...
│   │   ├── presets/            # terrain (dimensions + couleurs par défaut) et effectif (5v5 stack V/H, empty...)
│   │   └── interpolation.ts   # calcul des positions interpolées entre 2 frames
│   ├── state/                 # stores Zustand
│   │   ├── actionEditorStore.ts
│   │   └── libraryStore.ts    # persistance locale des actions sauvegardées
│   ├── components/
│   │   ├── field/              # rendu Konva du terrain + entités
│   │   ├── editor/              # UI d'édition (timeline de frames, panneau d'ajout de joueur...)
│   │   └── playback/            # contrôles de lecture (pas à pas / fluide, vitesse)
│   ├── i18n/
│   │   └── fr.json
│   ├── pages/                  # écrans (édition, lecture) si routing simple nécessaire
│   ├── App.tsx
│   └── main.tsx
├── docs/ROADMAP.md
├── CLAUDE.md
├── vite.config.ts
└── package.json
```

## 3. Rendu du terrain (approche technique)

- Un `Stage` Konva dimensionné via un conteneur responsive (observation du `ResizeObserver` du parent), qui calcule un ratio pixels/pourcentage à chaque redimensionnement. Le `Stage` tient dans l'espace disponible en largeur **et** en hauteur (comme `object-fit: contain`, voir §8) : si le conteneur a une hauteur définie (ex. `.field-stage` dans une colonne flex) et que la hauteur "dérivée de la largeur" la dépasserait, la largeur est recalculée à partir de la hauteur disponible à la place.
- Le terrain (lignes, en-buts) est dessiné à partir de `FieldConfig` (proportions **et couleurs**, jamais de valeurs en dur dans les composants) : `resolveFieldColors(config.colors)` retourne les couleurs personnalisées si présentes, sinon `DEFAULT_FIELD_COLORS` (gris terrain / bleu en-but / gris ardoise pour les lignes / gris soutenu hors-ligne — voir `docs/DATA_MODEL.md`).
- **Marge sideline** (voir `docs/DATA_MODEL.md`, section "Marge sideline") : `computeVisibleXRangePercent(fieldConfig)` (dans `src/domain/geometry.ts`) renvoie la plage `{ min, max }` en % à rendre sur l'axe largeur — toujours une étendue de 100 points de pourcentage, décalée (`{marginPercent, 100+marginPercent}`) plutôt qu'élargie quand `sidelineMarginMeters > 0`, pour n'afficher qu'une seule sideline (celle du côté x=100). `toX` mappe cette plage — et non plus systématiquement `[0,100]` — sur la largeur du `Stage` ; le rectangle terrain, les en-buts et le contour de lignes sont dessinés sur `[toX(0), toX(100)]` uniquement (partiellement hors-canvas côté x=0 quand la marge est active), la zone hors-ligne (`colors.outOfBounds`) occupant le reste du canvas. Comme l'étendue de la plage reste toujours 100, `toX`/`toY` gardent la même échelle qu'en l'absence de marge : le rayon des entités/du disque (calculé par rapport à `width`, la largeur totale du `Stage`) ne dépend donc jamais du réglage de marge.
- Chaque `Entity` est un `Circle` Konva draggable ; le `Disc` un `Circle` plus petit, toujours draggable (position toujours libre — voir §8).
- Les flèches de trajectoire (mode lecture) sont calculées à partir de deux frames consécutives : ligne droite au MVP (voir PRD, courbes bézier en post-MVP).

## 4. Interpolation (mode fluide)

- Fonction pure `interpolate(frameA, frameB, t: number): Frame` dans `src/domain/interpolation.ts`, indépendante de React/Konva → testable unitairement.
- Le contrôleur de lecture pilote `t` via `requestAnimationFrame`, en respectant `transitionMs` (par frame) et `speedMultiplier`.
- Pour chaque entité/disque du segment, `interpolate` choisit entre deux fonctions selon la présence d'une entrée dans `frameB.incomingCurves` (voir `docs/DATA_MODEL.md` §8) :
  - `lerp(p0, p1, t)` — comportement par défaut, ligne droite.
  - `quadraticBezier(p0, controlPoint, p1, t)` — utilisée uniquement pour l'entité/le disque concerné par une entrée `incomingCurves`, sans affecter les autres entités du même segment.
- Ces deux fonctions vivent aussi dans `src/domain/interpolation.ts`, pures et testées indépendamment.

## 5. Persistance

- `libraryStore.ts` encapsule toute la lecture/écriture localStorage (voir `docs/DATA_MODEL.md` §6) : aucun composant ne doit accéder à `localStorage` directement.
- Export/Import JSON : fonctions pures de sérialisation/validation, réutilisables telles quelles si un backend est ajouté plus tard (le format d'échange ne change pas).

## 6. Déploiement (GitHub Pages)

- GitHub Actions : build Vite (`base: '/ultimate-playbook/'`) sur push sur `main`, déploiement via `actions/deploy-pages` (ou `peaceiris/actions-gh-pages`).
- Le `base` path et toute config d'hébergement vivent uniquement dans `vite.config.ts` / le workflow CI — jamais codés en dur ailleurs dans l'app (voir principe d'agnosticisme d'hébergement ci-dessus).

## 7. Arborescence de frames (branches)

Voir `docs/DATA_MODEL.md` §9 pour le modèle de données (un arbre, pas une liste). Côté code :

- `src/domain/tree.ts` (pur, testé, indépendant de React/Konva) : `getChildren(frames, parentId)`, `getRootFrame(frames)`, `isForkFrame(frames, frameId)` (plus d'un enfant), `resolvePath(frames, choices)` — suit l'arbre depuis la racine en résolvant chaque embranchement via une map `{ frameId d'embranchement → id de l'enfant choisi }`.
- Éditeur (Phase 4) : deux actions distinctes dans l'UI — "frame suivante" (continuation simple, crée un unique enfant) vs "ajouter une option depuis cette frame" (crée un second enfant, `branchLabel` obligatoire ; si le parent n'avait qu'un seul enfant jusque-là, celui-ci doit être labellisé rétroactivement au moment où le fork est créé, puisqu'un parent à plusieurs enfants exige un `branchLabel` sur chacun). La timeline se scinde visuellement en pistes parallèles à partir d'un embranchement (façon graphe de commits), chaque piste étiquetée par son `branchLabel`, plutôt qu'une simple bande linéaire — composant sensiblement plus complexe qu'un filmstrip.
- Lecture (Phase 5) : pas à pas → choix inline entre les `branchLabel` disponibles à un embranchement (au lieu d'un bouton "suivant" unique) ; fluide → sélection d'un chemin complet via `resolvePath` avant de lancer l'animation, jamais d'interruption en cours de lecture.

## 8. Journal de décisions

À compléter au fil du projet (format court : date, décision, raison). Sert de mémoire pour les agents qui reprennent le projet plus tard.

- **2026-08-24** — Choix du modèle d'action par **keyframes/snapshots** plutôt qu'un mode "enregistrement" en temps réel : plus simple à éditer, correspond nativement au besoin de lecture pas-à-pas, et rend l'interpolation fluide triviale à calculer.
- **2026-08-24** — Konva/react-konva retenu plutôt que SVG "à la main" ou Canvas brut : gère drag & drop, tactile et animation nativement, évite de réimplémenter ces briques.
- **2026-08-24** — Pas de backend au MVP : persistance locale + export/import JSON, pour rester déployable en statique pur (GitHub Pages) et utilisable hors-ligne.
- **2026-08-24** — Trajectoire courbe du disque intégrée au périmètre MVP (déplacée depuis le hors-scope), modélisée par un point de contrôle de Bézier quadratique par segment plutôt qu'une cubique ou un dessin libre : la contrainte "facile à éditer" prime, et un seul point réutilise l'interaction de drag déjà présente sur les entités. Structure générique (`incomingCurves`, cf. `docs/DATA_MODEL.md` §8) pour pouvoir étendre aux joueurs plus tard sans migration. Implémentation isolée en Phase 6 de `docs/ROADMAP.md`, indépendante du reste du MVP.
- **2026-08-24** — Couleurs par défaut du terrain fixées à gris (`#D9DBDE`) + bleu en-but (`#3D6FB4`) + lignes gris ardoise (`#4B4F58`) : neutre pour le terrain (ne concurrence pas les entités colorées), convention indoor pour l'en-but. Stockées comme un objet `colors?` optionnel sur `FieldConfig` avec fallback vers `DEFAULT_FIELD_COLORS`, pour rester configurables sans changement de schéma même si l'UI de personnalisation n'arrive qu'après le MVP.
- **2026-08-24** — Marge sideline modélisée comme une **extension de la plage de coordonnées existante** (`x` peut sortir de `[0,100]`) plutôt qu'un système de coordonnées ou une vue alternative séparée : `x=0`/`x=100` gardent toujours le même sens (les lignes de touche réelles), donc un export JSON reste non-ambigu quel que soit le réglage d'affichage. `sidelineMarginMeters` (défaut `0`, aucun changement pour les actions qui n'en ont pas besoin) contrôle uniquement combien de cette plage étendue le rendu réserve visuellement. Cette même plage sert aussi de zone "draggable" pour le point de contrôle d'une trajectoire courbe (§8 de `docs/DATA_MODEL.md`), sans mécanisme séparé.
- **2026-08-24** — `Action.frames` remodélisé en **arbre** (`parentId`/`siblingOrder`/`branchLabel` plutôt qu'un simple `order`) pour supporter les plays à embranchements (ex. sortie de ligne avec choix around/strike), un cas jugé courant et non un edge case. Fusion de branches (DAG) explicitement exclue : un arbre pur reste beaucoup plus simple à éditer et à lire. Décidé avant toute persistance/export réel, donc pas de migration nécessaire (schéma révisé directement, `schemaVersion` inchangé). Contrairement à la trajectoire courbe du disque, ce changement touche le schéma central dès la Phase 1/2 déjà codées ; un correctif rétroactif de `src/domain/models.ts` et de la démo Phase 2 est nécessaire avant d'attaquer la Phase 4 (voir `docs/ROADMAP.md`).
- **2026-08-25** — Marge sideline revue après retour utilisateur : la première version étendait la plage visible symétriquement des deux côtés, ce qui rétrécissait tout le rendu (terrain + entités) pour tenir dans la même largeur de conteneur — jugé "stupide" (un play "longue ligne" ne concerne qu'un seul bord). Remplacé par une plage **décalée** plutôt qu'élargie (`[marginPercent, 100+marginPercent]`, toujours 100 points de pourcentage d'étendue) : une seule sideline visible, taille des entités/du terrain inchangée quel que soit le réglage, au prix d'un bord opposé du terrain qui sort du cadre visible (compromis jugé approprié vu l'usage ciblé). Le point de contrôle d'une trajectoire courbe (§7, `docs/DATA_MODEL.md` §8) profite du même changement sans modification supplémentaire.
- **2026-08-25** — `Disc.heldBy`/`Entity.hasDisc` retirés du modèle (Phase 8) : à l'usage, aucun joueur ne se déplace disque en main à l'Ultimate (le porteur est toujours à l'arrêt), donc "attacher" le disque à une entité n'apportait qu'un confort marginal (suivre la position si l'entité est repositionnée) contre un vrai coût UX — le disque tenu n'était pas draggable tant qu'on ne cliquait pas sur "Libérer le disque" (`!disc.heldBy` dans `DiscMarker.tsx`), ce qui créait un état bloquant confus. Le disque est désormais **toujours en position libre** (`Disc = { x: number; y: number }`, plus jamais optionnel), positionné uniquement par glisser-déposer direct ; les boutons "Donner le disque"/"Libérer le disque" et les actions `assignDiscTo`/`freeDisc` du store sont supprimés. Le décalage visuel du disque "en main" (pour ne pas masquer le label du porteur) continue de fonctionner sans changement : il était déjà basé sur la coïncidence de position (`findColocatedEntity`, voir la note Phase 5 ci-dessus) plutôt que sur `heldBy`. Écart par rapport à `docs/DATA_MODEL.md` tel qu'écrit initialement (mis à jour en conséquence) ; pas de migration prévue pour les actions déjà sauvegardées en localStorage avec un disque "tenu" — elles se rechargent (le champ `heldBy` orphelin est simplement ignoré), mais peuvent nécessiter de repositionner le disque manuellement sur ces frames.
- **2026-08-25** — Placement par défaut du défenseur d'un handler (`D1`/`D2` dans les presets d'effectif) corrigé : l'ancien décalage uniforme (`MARK_DX`/`MARK_DY`, pensé pour éviter le chevauchement visuel des cutters alignés en stack) plaçait le marqueur simplement "à côté" du handler à la même profondeur, ce qui ne correspond pas à un marquage réaliste. Un marqueur sur le porteur/futur porteur se place devant lui, entre lui et la direction d'attaque (`y=0`), avec un léger décalage latéral pour figurer une force — nouvelles constantes dédiées `HANDLER_MARK_DX`/`HANDLER_MARK_DY` dans `src/domain/presets/roster.ts`, distinctes de celles des cutters.
- **2026-08-25** — Plan initial pour le "terrain plein écran mobile" (Phase 8) revu après un essai visuel : l'idée de départ (toolbar d'ajout de joueur en boutons **flottants directement sur le canvas**, à la façon d'une palette d'outils) a été abandonnée dès la première capture d'écran — une toolbar à fond opaque posée sur le terrain masquait entièrement les joueurs situés dessous, ce qui est inacceptable pour un outil dont l'usage central est de lire/positionner précisément des joueurs. Remplacé par une approche plus sûre : le "overlay" se fait au niveau de la mise en page (colonne flex `100dvh`, chrome secondaire replié/compacté, aucun scroll de page) plutôt qu'au niveau du rendu (rien n'est jamais peint par-dessus le dessin du terrain lui-même). Le menu ☰ (nom/export/nouvelle action) reste, lui, un vrai dropdown en overlay au-dessus du terrain — validé sans problème car il ne s'affiche que ponctuellement, sur demande explicite.
- **2026-08-25** — Correctif critique post-déploiement : la mise en page "plein écran mobile" ci-dessus avait été validée avec un viewport Playwright de test **artificiellement haut** (844px — la hauteur totale théorique de l'écran, sans tenir compte de la barre d'adresse du navigateur), au lieu de la hauteur réellement visible (~660-730px sur un téléphone réel, adresse visible). Retour utilisateur immédiat : "interface complètement pétée", terrain minuscule, aucun scroll possible. Cause racine : `Field.tsx` dérivait systématiquement sa hauteur de sa largeur (`height = width × ratio`), sans jamais tenir compte de l'espace vertical réellement disponible — sur un vrai écran de téléphone, un terrain en demi-largeur de portrait (ratio ~1.67) réclame ~620px de haut à lui seul, largement plus que ce qu'il restait une fois la timeline de frames et la toolbar décomptées, forçant un scroll interne caché dans `.field-stage` (donc invisible/non découvrable pour l'utilisateur). Corrigé à la racine plutôt qu'en rustine CSS : `Field.tsx` dimensionne désormais le `Stage` pour tenir dans l'espace disponible en largeur **et** en hauteur (`object-fit: contain`), en repliant la largeur si la hauteur dérivée dépasserait le conteneur ; `.editor` devient une "app shell" plein écran unique (`height:100dvh`, colonne flex) appliquée à **toutes** les tailles d'écran plutôt que juste sous une media query mobile — supprime la duplication de mise en page desktop/mobile qui avait initialement masqué le problème. Leçon retenue : toujours valider un layout "plein écran mobile" avec un viewport Playwright réaliste (`devices["iPhone 13"]` etc., hauteur visible barre d'adresse comprise), jamais avec la hauteur d'écran brute.
- **2026-08-25** — Passe de design visuel ("clean et moderne", retour utilisateur direct) : introduction de jetons de design CSS (`--accent`, `--surface`, `--border`, `--radius*`, `--shadow*`) dans `src/index.css`, remplacement des couleurs codées en dur (jaune/or `#f2c94c`, gris divers) par cette palette cohérente (bleu `--accent`) déclinée clair/sombre, coins arrondis + ombre légère sur les panneaux (menu, sélection, timeline) et les boutons, variante `.primary` pour le bouton d'action principal ("Commencer"). Portée volontairement limitée au chrome UI (boutons, panneaux, formulaires) : les couleurs du terrain/des entités (`theme.ts`, `presets/fieldColors.ts`) ne sont pas concernées, elles répondent à un besoin de lisibilité tactique distinct.
- **2026-08-25** — Retour au véritable overlay sur le canvas, revenant sur la décision du 2026-08-25 ci-dessus ("Plan initial... revu après un essai visuel") : l'utilisateur a explicitement demandé que le terrain **prenne tout l'écran** et que le reste de l'UI soit **par-dessus, en overlay** — pas un partage d'espace en flexbox, même compact. La préoccupation initiale (une pastille opaque masquant des joueurs) est résolue non pas en évitant l'overlay sur le canvas, mais en le rendant **translucide et flouté** (`.overlay-bar` : `background: color-mix(in srgb, var(--surface) 82%, transparent); backdrop-filter: blur(14px)`) façon "verre dépoli" — les joueurs situés dessous restent visibles en transparence plutôt que d'être complètement cachés. Architecture : `.field-stage` devient `position: absolute; inset: 0` (occupe tout `.editor`, qui n'est plus qu'un conteneur `position: relative; height: 100dvh`), et tout le chrome (topbar, menu, toolbar, timeline, panneau de sélection, contrôles de lecture) passe en `position: fixed`, regroupé dans deux piles `.overlay-stack` (haut/bas, `pointer-events: none` sur la pile elle-même pour laisser passer les clics vers le terrain entre deux pastilles, `pointer-events: auto` restauré sur chaque pastille `.overlay-bar`). Limite connue et acceptée : un joueur positionné exactement sous une pastille (ex. un handler proche du bas de terrain, sous la timeline) n'est temporairement pas cliquable/déplaçable tant qu'il reste dans cette zone — compromis direct du choix explicite de l'utilisateur, à revisiter si ça s'avère gênant en usage réel (ex. timeline repliable par défaut).
- **2026-08-25** — La limite "connue et acceptée" ci-dessus s'est révélée être le vrai problème : retour utilisateur "c'est complètement inutilisable justement parce que les joueurs ne sont plus clickable". `pointer-events: auto` sur une pastille translucide bloque bel et bien les clics/drags sur ce qu'il y a en dessous, même si c'est visible par transparence — la translucidité résout l'aspect visuel (voir le joueur), pas l'aspect interaction (le toucher). Décision : passage en **mode DESIGN** avant tout nouveau code — réflexion UX écrite avec l'utilisateur, maquette interactive (`Editor Layout Proposal`, artifact), puis spécification formelle (`docs/PRD.md` §4.8) validée AVANT implémentation. Principe retenu, distinct des deux tentatives précédentes : au lieu de faire flotter TOUT le chrome sur le canvas (avec le risque d'y bloquer durablement des interactions), seuls deux éléments minuscules restent en overlay permanent sur le terrain — les pastilles Éditer/Jouer et ⋯ (un coin d'écran chacune, risque de blocage jugé négligeable et accepté explicitement par l'utilisateur) — tandis que la barre Frames, elle, retrouve un **espace dédié** (`flex: none` dans la colonne de `.editor`, pas de recouvrement du terrain) : aucun joueur n'y est donc plus jamais durablement inatteignable, la vraie source du problème. Le menu ⋯, quand ouvert, reste un panneau opaque avec voile — accepté sans réserve par l'utilisateur car transitoire (on n'édite pas le terrain pendant qu'on renomme l'action). Nouvelle fonction pure `computeCurrentPathView` (`src/domain/tree.ts`) pour la barre Frames : chemin depuis la racine jusqu'à la frame courante, étendu sans ambiguïté dans les deux sens, pas la vue "arbre complet" de l'ancien `BranchChain.tsx` (retiré, devenu inutilisé) — naviguer vers une branche déjà explorée ailleurs se fait en deux temps (remonter à l'embranchement, puis choisir) plutôt qu'en raccourci direct, compromis délibéré pour garder cette barre toujours visible sans jamais afficher tout l'arbre.
- **2026-08-26** — Orientation portrait/paysage du terrain (`docs/PRD.md` §4.8bis) : `toX`/`toY`/`fromX`/`fromY` (`Field.tsx` et les composants qui les reçoivent en props — `DiscMarker`, `GhostFrame`, `TrajectoryArrows`, `DiscCurveEditor`) sont passés d'une signature à un seul argument (dépendant d'un seul axe du modèle, largeur **ou** longueur) à **deux arguments, toujours `(widthPercent, lengthPercent)` dans cet ordre**, plutôt que de garder deux fonctions à un argument et de basculer laquelle des deux coordonnées du modèle passer à chaque site d'appel selon l'orientation. Raison : en paysage, l'axe écran X est piloté par la longueur (pas la largeur) — sans ce changement de signature, chaque site d'appel (une douzaine, répartis sur 5 fichiers) aurait dû connaître l'orientation courante et choisir la bonne coordonnée à passer, alors qu'avec la signature à deux arguments, `toX`/`toY` décident seuls en interne laquelle des deux utiliser (voir `domain/geometry.ts` `projectToScreen`/`unprojectFromScreen`) et les composants enfants restent strictement inchangés dans leur logique, seul le nombre d'arguments passés change. Le rayon des marqueurs (entité/disque, `ENTITY_RADIUS_RATIO`/`DISC_RADIUS_RATIO`) est calculé à partir de `widthAxisPixelSpan` (largeur du Stage en portrait, hauteur en paysage) plutôt que systématiquement de la largeur du Stage : sans ça, un terrain paysage large mais peu haut aurait produit des marqueurs disproportionnés. Les rectangles axe-alignés (terrain, bandes d'en-but) utilisent une nouvelle fonction `projectRect` (deux coins du modèle → rectangle écran normalisé) plutôt que de dupliquer la logique portrait/paysage à chaque tracé. Réglage volontairement **non persisté** (état Zustand simple, `orientation`/`setOrientation` dans `actionEditorStore.ts`, absent de `Action`/`FieldConfig` et exclu du `subscribe` de sauvegarde automatique) : toujours "portrait" au chargement d'une page, choix produit explicite pour garder le réglage simple au MVP.
- **2026-08-26** — Panneau Frames latéral sur grand écran (`docs/PRD.md` §4.8ter) : bascule entre la barre du bas (`FrameTimeline.tsx`, chemin courant seul) et un nouveau panneau vertical (`FrameTreePanel.tsx`, arbre complet) décidée par une **mesure réelle** (`ResizeObserver` sur `.editor-main`, comparant la largeur de `Field` une fois ajustée via `fitFieldStageSize` à la largeur totale disponible) plutôt qu'un point de rupture CSS fixe — le "slack" en question dépend de l'orientation courante et du ratio du terrain, pas seulement de la largeur d'écran, donc un breakpoint fixe aurait mal généralisé (ex. un terrain complet, plus large, laisse moins de côté qu'un demi-terrain à largeur d'écran égale). Nouvelle fonction pure `computeFullTreeRows` (`domain/tree.ts`) : parcours en profondeur (préfixe) de tout l'arbre, à plat, avec deux compteurs distincts par ligne — `visualDepth` (n'augmente qu'à une branche **labellisée**, même une avec une seule option pour l'instant, même convention que `computeCurrentPathView` suite au retour "aucun feedback à la création de la première branche" ; documenté par un test dédié) et `generation` (position depuis la racine, incrémentée à chaque frame, sert de numérotation d'affichage). `BRANCH_COLOR_COUNT` déplacé de `FrameTimeline.tsx` vers `domain/tree.ts` (exporté) : les deux vues doivent attribuer la même couleur à une même branche, donc la constante ne doit exister qu'à un seul endroit. Le panneau réutilise les actions rapides de la barre du bas (frame suivante, nouvelle branche) pour rester fonctionnellement équivalent — seule la présentation change.
- **2026-08-27** — Planification Phases 9/10 (`docs/PRD.md` §4.9/§4.10), suite à un retour direct de l'utilisateur ("les menus sont super moches", "le formulaire ressemble à un formulaire", "j'aimerais flagger les plays ou avoir un système de hiérarchie"). Trois décisions produit actées après discussion :
  1. **`Action.tags: string[]` remplacé par trois champs optionnels `category?`/`system?`/`variant?`** (`docs/DATA_MODEL.md` §4) plutôt qu'une taxonomie fixe (listes déroulantes prédéfinies) ou des tags plats avec filtre multi-critères. Raison : `tags` n'a jamais été exposé dans aucune UI (champ mort depuis son introduction en Phase 1), donc aucune migration réelle à gérer ; des champs structurés à 3 niveaux permettent une navigation en drill-down ("Attaque → Vertical → Longue ligne", le besoin exprimé) sans imposer un vocabulaire figé à l'utilisateur — chaque niveau reste du texte libre, avec suggestions des valeurs déjà utilisées plutôt qu'un enum fermé.
  2. **Pas de système de flags/favoris pour l'instant** : le classement hiérarchique à 3 niveaux couvre le besoin exprimé de retrouver un play rapidement ; ajouter un système de flags par-dessus aurait dupliqué cette fonction sans qu'un besoin distinct soit identifié. À réévaluer si l'usage réel montre que ça manque.
  3. **L'écran Bibliothèque devient l'écran d'accueil de l'application**, remplaçant la reprise automatique de la dernière action éditée (`getLastActiveActionId`/`App.tsx`, en place depuis la Phase 7) : avec plusieurs actions désormais visibles et ouvrables individuellement, rouvrir automatiquement la dernière sans passer par la bibliothèque serait devenu une incohérence de navigation (on ne saurait jamais sur quel écran on atterrit). Icônes des tuiles (terrain/effectif en Phase 9, catégories de bibliothèque en Phase 10) en **SVG dédiés** plutôt qu'en emoji, choix explicite de l'utilisateur pour un rendu plus soigné/cohérent.
- **2026-08-27** — Implémentation Phase 9 (`docs/PRD.md` §4.9). Choix notables :
  - Le système de bouton reste une seule règle `button` globale (pas de composant `<Button>` React dédié) : la quasi-totalité des boutons de l'app sont déjà stylés via des classes spécifiques (`.frame-chip`, `.branch-pill`, `.chip-add`, `.mode-switch button`, `.orientation-switch button`...) qui redéfinissent explicitement `background`/`border`/`box-shadow` — seule la base CSS partagée (fond, ombre, radius, transitions, focus visible) manquait de soin. Un composant React aurait ajouté une couche d'indirection sans réel bénéfice vu que ces boutons spécifiques ne passeraient pas par lui de toute façon.
  - `NewActionSetup.tsx` : la case "marge sideline" (orthogonale au type de terrain) devient une 3e tuile terrain à part entière ("Demi-terrain + sideline"), et "Terrain indéfini" disparaît de cet écran — les deux décidés explicitement avec l'utilisateur avant implémentation (voir l'entrée de planification ci-dessus). `FieldPresetKey` (`"half" | "full" | "sideline"`) reste un type local à l'écran de setup, traduit en `FieldType` + `sidelineMarginMeters` au moment de `handleStart` — pas de changement du modèle de données `FieldConfig`.
  - `SetupTile.tsx` : un seul composant de tuile réutilisé pour les 6 choix (3 terrain + 3 effectif), pas deux composants séparés — la structure (icône + libellé + état sélectionné) est strictement identique dans les deux cas.
  - Icônes (`components/icons/SetupIcons.tsx`) : SVG inline en composants React (pas de fichiers `.svg` externes) pour rester cohérent avec le reste du code (pas de pipeline d'assets à ce stade) et utiliser `currentColor`, qui hérite automatiquement la couleur de la tuile (état sélectionné/non sélectionné) sans dupliquer de styles par variante.
- **2026-08-28** — Trois retours utilisateur directs post-Phase 9, traités ensemble :
  1. **Libellé "Frames" → "Étapes"** : "Frames" ne voulait rien dire en français. Seules les valeurs i18n de `editor.frames.*` (`fr.json`) changent — le code (type `Frame`, `FrameTimeline.tsx`, `FrameTreePanel.tsx`, `computeCurrentPathView`...) reste en anglais, conformément à `CLAUDE.md` (code en anglais, contenu utilisateur en français via i18n). "Étape" retenu plutôt que "Image-clé" (le terme déjà utilisé pour le concept dans `docs/PRD.md` §3) : trop long pour un libellé de bouton/aria-label répété partout ("+ Image-clé suivante" contre "+ Étape suivante").
  2. **Marquage du porteur du disque (D1) mirroré à gauche** (`fiveVFiveVerticalStackPreset`/`fiveVFiveHorizontalStackPreset`, `domain/presets/roster.ts`) : jusqu'ici `H1.x + HANDLER_MARK_DX`, comme les défenseurs du stack — changé en `H1.x - HANDLER_MARK_DX`. Une vraie force sur l'effectif pousse tous les défenseurs du même côté ; marquer le porteur à droite (même côté que le stack) n'avait pas de sens tactique. D2 (marqueur de H2) inchangé — seul D1 était concerné par ce retour.
  3. **Menu ⋯ déployé en permanence sur grand écran** (`docs/PRD.md` §4.8quater) : contenu du menu extrait de `PositionEditor.tsx` vers `EditorActionsPanel.tsx`, qui lit directement le store (comme `FrameTimeline`/`FrameTreePanel`/`FrameActionsMenu`) plutôt que de recevoir ses données en props — seules `showGhostFrame`/`onToggleGhostFrame` (état local à `PositionEditor.tsx`, absent du store) sont passées en props. Réutilise le **même** signal `useSidePanel` que le panneau Étapes latéral (§4.8ter, une seule mesure d'espace pour les deux bascules) plutôt qu'un second `ResizeObserver` : sur grand écran, `EditorActionsPanel` s'affiche en permanence dans une nouvelle colonne `.editor-side-column` (empilée au-dessus du panneau Étapes), le bouton ⋯ ne se rend plus du tout ; sur écran étroit (y compris un grand écran en paysage, si le terrain y occupe alors presque toute la largeur — vérifié : un demi-terrain en paysage sur 1440px de large ne laisse plus assez de côté), le comportement d'origine (bouton replié + panneau flottant) reste inchangé.
- **2026-08-28** — Repère de position dans l'arbre en mode lecture (`docs/PRD.md` §4.4), retour utilisateur direct ("il faudrait qu'on ait visuellement la représentation de l'arbre de Frames et qu'on sache où on en est, pas à pas comme fluide"). `PlaybackFrameTrail.tsx` réutilise telle quelle la fonction `computeCurrentPathView` et les classes CSS de la barre Étapes de l'éditeur (`.frame-timeline`, `.frame-chip`, `.branch-pill`, `.branch-color-N`) — pas de nouvelle logique de rendu, seulement une nouvelle façon de la consommer. Rendu en `<span>` plutôt qu'en `<button>` (contrairement à `FrameTimeline.tsx`) : volontairement non cliquable, pour ne pas ouvrir un second mécanisme de navigation qui court-circuiterait celui propre à chaque mode de lecture (avancer pas à pas, ou choisir tout le chemin puis lancer l'animation en fluide — jamais d'interruption en cours d'animation, §4.4). Nécessite d'ajouter `display: inline-flex; align-items: center; justify-content: center;` à `.frame-chip` (jusqu'ici implicite via le rendu par défaut d'un `<button>`, donc invisible tant que seul du `<button>` l'utilisait) pour que le chiffre reste centré une fois rendu en `<span>`. En mode fluide, la frame "courante" à passer au composant a deux définitions selon la phase : `lastResolved.id` tant que le chemin n'est pas entièrement choisi (le `segmentIndex` d'animation reste à 0 avant que la lecture ne démarre, donc l'utiliser aurait affiché la racine même en pleine sélection de branche) ; `path[segmentIndex].id` une fois le chemin complet (reflète alors la progression réelle de l'animation).
- **2026-08-28** — Deux retours utilisateur directs supplémentaires :
  1. **Zone de glisser-déposer du disque étendue** ("le disque est très dur à drag sur mobile") : `DiscMarker.tsx` ajoute `hitStrokeWidth` (propriété Konva dédiée à exactement ce cas — étendre la zone de détection sans toucher au rendu visuel, puisque Konva calcule les hit-tests sur un canvas séparé du rendu affiché) calculé pour garantir un rayon de détection d'au moins 22px (cible ~44px de diamètre, `docs/PRD.md` §4.7) même quand le rayon visuel du disque est bien plus petit — ce qui est systématiquement le cas sur mobile (`DISC_RADIUS_RATIO` ne dépend que de la largeur du Stage). Vérifié par un glisser démarré à 15px du centre visuel (donc hors du disque lui-même sur un écran de téléphone, où son rayon mesure ~7-8px) : le disque suit bien le geste.
  2. **Flèches de trajectoire limitées aux joueurs offense** ("ça pollue un peu [...] si on est sur un play d'attaque, on n'affiche pas les flèches de mouvement des défenseurs") : deux options envisagées — masquer les flèches défense inconditionnellement, ou introduire un vrai champ "type de play" (attaque/défense) sur `Action` pour décider dynamiquement quel camp affiche ses flèches. Choix explicite avec l'utilisateur pour la première option (`TrajectoryArrows.tsx` filtre `nextFrame.entities` sur `team === "offense"` avant de calculer les flèches joueurs) : l'outil est conçu autour de plays d'attaque de bout en bout (`docs/PRD.md` §1, "Vision"), les défenseurs n'y sont que du contexte de marquage — un champ "type de play" aurait été une vraie fonctionnalité nouvelle sans besoin identifié pour l'instant, à réévaluer si un usage réel de plays défensifs émerge.
